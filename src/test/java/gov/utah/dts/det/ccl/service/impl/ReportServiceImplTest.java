package gov.utah.dts.det.ccl.service.impl;

import org.testng.annotations.Test;

@Test
public class ReportServiceImplTest {

	/*@Test
	public void testGetReports() {
		ITextReportConfig config = new ITextReportConfig();
		config.setKey("test1");
		config.setName("Test Report");
		config.setDescription("This is a test report");
		config.setReportClass("TestClass");
		
		Set<ITextReportConfig> configs = new HashSet<ITextReportConfig>();
		configs.add(config);
		
		ReportServiceImpl repServ = new ReportServiceImpl();
		repServ.setConfigs(configs);
		Collection<ITextReportConfig> reports = repServ.getReports();
		Assert.assertTrue(reports.size() == 1);
		Assert.assertTrue(reports.contains(config));
	}*/
}