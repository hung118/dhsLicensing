<%@ taglib prefix="s" uri="/struts-tags"%>
<%@ taglib prefix="display" uri="http://displaytag.sf.net"%>
<fieldset>
	<legend>Interpretation Manual</legend>
	We are reworking the way the interpretation manual is rendered and stored so it is currently unavailable.
	<!--<display:table name="rules" id="rules" class="tables">
		<display:column title="Facility Type">
			<s:url id="ruleSectionsUrl" action="sections" includeParams="false">
				<s:param name="rule.id" value="#attr.rules.id"/>
			</s:url>
			<s:a href="%{ruleSectionsUrl}">
				<s:property value="#attr.rules.facilityType.value"/>
			</s:a>
		</display:column>
		<display:column title="Rule #">
			<s:property value="#attr.rules.number"/>
		</display:column>
			<display:column title="Rule Name">
				<s:property value="#attr.rules.name"/>
			</display:column>
	</display:table>-->
</fieldset>